---
phase: 03-data-layer
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01-PLAN
files_modified:
  - server/services/dataCache.ts
  - server/index.ts
  - server/scripts/refreshBallDontLieCache.ts
  - server/data/coaches-seed.json
  - docs/data/cache-operations.md
autonomous: true
must_haves:
  truths:
    - "Server startup warms data cache in background without blocking request handling."
    - "Runtime endpoints consume cache/seed files and avoid request-path outbound API calls."
    - "Coach profile seed includes required tendency tags for all head coaches."
  artifacts:
    - path: "server/services/dataCache.ts"
      provides: "Disk cache load, warm-up, read APIs, and refresh orchestration."
    - path: "server/scripts/refreshBallDontLieCache.ts"
      provides: "Manual/cron-safe cache refresh entrypoint."
    - path: "server/data/coaches-seed.json"
      provides: "Coach profiles with pace, scheme, youth, driver, and shooter tags."
  key_links:
    - from: "server/index.ts"
      to: "server/services/dataCache.ts"
      via: "startup warm-up trigger"
      pattern: "warm"
    - from: "server/services/dataCache.ts"
      to: "server/data/coaches-seed.json"
      via: "coach profile loading"
      pattern: "coach"
---

<objective>
Deliver DATA-02 and DATA-03 by implementing disk cache warm-up/refresh behavior and shipping a complete real-coach tendency seed artifact.

Purpose: Establish low-latency, deterministic server data access and complete coach metadata required for teaching and simulator phases.
Output: Cache service, refresh script, startup warm-up hooks, and validated coach seed file.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@server/index.ts
@server/services/handlers-v2.ts
@server/data/nba-seed.json
</context>

<tasks>
<task type="auto">
  <name>Task 1: Build disk cache service with warm-up lifecycle</name>
  <files>server/services/dataCache.ts, server/index.ts</files>
  <action>
1. Implement cache loader for player/team/coach datasets from local artifacts.
2. Add startup warm-up call in server bootstrap that runs async and logs completion metrics.
3. Expose cache read helpers for downstream routes/services.
4. Ensure cache misses fall back to seed artifacts, not live API calls.
  </action>
  <verify>
Start server with empty in-memory cache and confirm warm-up logs plus successful cache-backed responses.
  </verify>
  <done>Cache service initializes on startup and serves data without request-path API dependence.</done>
</task>
<task type="auto">
  <name>Task 2: Implement refresh script and operational docs</name>
  <files>server/scripts/refreshBallDontLieCache.ts, docs/data/cache-operations.md</files>
  <action>
1. Add refresh script that regenerates cache files from seed pipeline outputs.
2. Make refresh idempotent and safe to run repeatedly.
3. Document refresh timing, success indicators, and rollback path for bad data.
  </action>
  <verify>
Run refresh script twice in a row; confirm no corruption and stable cache state after both runs.
  </verify>
  <done>Cache refresh process is safe, repeatable, and documented for operators.</done>
</task>
<task type="auto">
  <name>Task 3: Create complete coach profile seed with required tendency tags</name>
  <files>server/data/coaches-seed.json</files>
  <action>
1. Populate each current NBA head coach with required fields:
   - pace preference
   - scheme tag
   - youth development flag
   - driver-friendly flag
   - shooter-friendly flag.
2. Validate field completeness and unique coach/team mapping.
3. Add lightweight schema validation step before save.
  </action>
  <verify>
Run validation script and confirm 100% of coach entries include required tendency fields.
  </verify>
  <done>Coach seed artifact is complete and ready for simulator and lesson consumption.</done>
</task>
</tasks>

<verification>
- [ ] Startup warm-up runs and logs expected dataset counts.
- [ ] Request-path data lookups resolve via cache/seed sources.
- [ ] Refresh script is idempotent and operationally documented.
- [ ] Coach seed includes all required tendency tags for each entry.
</verification>

<success_criteria>
- DATA-02 and DATA-03 are complete with stable runtime behavior.
- Server no longer relies on outbound API calls for request-path data resolution.
- Coach tendency metadata is production-ready for downstream features.
</success_criteria>

<output>
After completion, create `.planning/phases/03-data-layer/03-02-SUMMARY.md`
</output>
