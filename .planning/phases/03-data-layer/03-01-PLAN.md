---
phase: 03-data-layer
plan: 01
type: execute
wave: 1
depends_on:
  - 01-05-PLAN
files_modified:
  - server/scripts/seedNbaIdentity.ts
  - server/scripts/seed_nba_identity.py
  - server/scripts/requirements-nba.txt
  - server/data/nba-seed.json
  - server/data/nba-seed.meta.json
  - server/package.json
  - docs/data/seeding-workflow.md
autonomous: true
must_haves:
  truths:
    - "NBA teams and active players are seeded at build time into deterministic local artifacts."
    - "Request-path server code does not directly fetch stats.nba.com for identity."
    - "Seed script output includes timestamped metadata for reproducibility."
  artifacts:
    - path: "server/scripts/seedNbaIdentity.ts"
      provides: "TS driver: offline stub or shells Python seed_nba_identity.py."
    - path: "server/scripts/seed_nba_identity.py"
      provides: "Build-time identity via nba_api (league dash + commonteamroster merge)."
    - path: "server/data/nba-seed.json"
      provides: "Canonical local snapshot consumed by runtime services."
    - path: "docs/data/seeding-workflow.md"
      provides: "Operator instructions for running and validating data seed refreshes."
  key_links:
    - from: "server/package.json"
      to: "server/scripts/seedNbaIdentity.ts"
      via: "seed command script"
      pattern: "seed"
    - from: "server/services/dataCache.ts"
      to: "server/data/nba-seed.json"
      via: "startup data load"
      pattern: "nba-seed"
---

<objective>
Deliver DATA-01 by creating deterministic build-time seed generation for active NBA teams and players, producing runtime-ready local artifacts.

Purpose: Runtime endpoints must consume local cached data so product behavior is stable and independent of third-party API latency.
Output: Seed script(s), seed artifacts, and operational runbook for refresh behavior.

**Living docs (post-phase):** Follow-on stats depth and external sourcing are tracked in [`docs/data/nba-stats-stack-delta-todos.md`](../../../docs/data/nba-stats-stack-delta-todos.md) and [`docs/data/external-data-sources.md`](../../../docs/data/external-data-sources.md).
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@server/scripts/scrape_nba_stats.py
@server/index.ts
</context>

<tasks>
<task type="auto">
  <name>Task 1: Implement nba_api build-time identity seed (Python + TS driver)</name>
  <files>server/scripts/seed_nba_identity.py, server/scripts/seedNbaIdentity.ts</files>
  <action>
1. Fetch current NBA team ids (e.g. league dash) and roster rows per team via `nba_api` (`commonteamroster`), with throttling between team calls.
2. Normalize fields used by app features (NBA stats team/player ids, positions, heights, team_id).
3. Drop incomplete/duplicate entries with explicit logging counters in metadata.
4. Add deterministic sort to ensure stable artifact diffs.
5. Provide `--offline` path: 30 teams, NBA stats TeamIDs, zero players (CI / no Python).
  </action>
  <verify>
Run `npm run seed:nba --workspace=server` (Python + `nba-api` installed) and `npm run seed:nba:offline --workspace=server`; verify counts and metadata `source` (`nba_stats_api` vs `offline_static`).
  </verify>
  <done>Script produces normalized, deterministic seed payload suitable for runtime loading.</done>
</task>
<task type="auto">
  <name>Task 2: Write seed artifacts and metadata for reproducibility</name>
  <files>server/data/nba-seed.json, server/data/nba-seed.meta.json</files>
  <action>
1. Write main snapshot file containing teams and players arrays (`schemaVersion` 2+ for NBA stats ids).
2. Write metadata file with run timestamp, source, record counts, endpoints summary, and schema version.
3. Validate artifact JSON shape before save.
  </action>
  <verify>
Inspect generated files and confirm metadata reflects snapshot counts and current schema version.
  </verify>
  <done>Seed artifacts are generated with traceable metadata and validated structure.</done>
</task>
<task type="auto">
  <name>Task 3: Register seed command and document refresh workflow</name>
  <files>server/package.json, docs/data/seeding-workflow.md, server/scripts/requirements-nba.txt</files>
  <action>
1. Add reproducible package scripts (`seed:nba`, `seed:nba:offline`) in server workspace.
2. Document Python dependency (`pip install -r server/scripts/requirements-nba.txt`), optional `PYTHON` env, refresh cadence, expected counts, rollback.
3. Include no-runtime-call rule in runbook to enforce DATA-01 boundary.
  </action>
  <verify>
Follow runbook from clean checkout and confirm a new engineer can regenerate artifacts without ad-hoc steps.
  </verify>
  <done>Seed workflow is discoverable, scriptable, and ready for team use.</done>
</task>
</tasks>

<verification>
- [ ] Seed script runs successfully and produces artifact files.
- [ ] Artifact output is deterministic and includes metadata.
- [ ] Runtime data paths can rely on local seed snapshot.
- [ ] Workflow documentation supports repeatable refresh execution.
</verification>

<success_criteria>
- DATA-01 is complete with deterministic build-time seed generation.
- Runtime code can load local snapshots without live stats.nba.com calls on the request path.
- Seed refresh process is documented and operationally safe.
</success_criteria>

<output>
After completion, create `.planning/phases/03-data-layer/03-01-SUMMARY.md`
</output>
