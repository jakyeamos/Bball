---
phase: 03-data-layer
plan: 03
type: execute
wave: 3
depends_on:
  - 03-02-PLAN
files_modified:
  - server/scripts/scrape_nba_stats.py
  - server/services/playerFeaturesMapping.ts
  - server/services/playerFeaturesMapping.test.ts
  - docs/data/player-feature-mapping.md
  - shared/schemas.ts
autonomous: true
must_haves:
  truths:
    - "Python scraper emits season-average inputs required by PlayerFeatures mapping."
    - "All 30 PlayerFeatures fields map explicitly from source stats or documented derivations."
    - "Mapping code and docs remain synchronized through test-backed validation."
  artifacts:
    - path: "server/services/playerFeaturesMapping.ts"
      provides: "Deterministic transform from source stats into 30-field PlayerFeatures model."
    - path: "server/services/playerFeaturesMapping.test.ts"
      provides: "Coverage test asserting no missing PlayerFeatures mappings."
    - path: "docs/data/player-feature-mapping.md"
      provides: "Field-by-field mapping table with formulas and source references."
  key_links:
    - from: "server/scripts/scrape_nba_stats.py"
      to: "server/services/playerFeaturesMapping.ts"
      via: "normalized stat payload handoff"
      pattern: "season"
    - from: "server/services/playerFeaturesMapping.test.ts"
      to: "shared/schemas.ts"
      via: "schema validation of mapped output"
      pattern: "PlayerFeatures"
---

<objective>
Deliver DATA-04 and DATA-05 by extending the existing `nba_api` scraper and implementing explicit full-coverage mapping into the 30-field PlayerFeatures schema.

Purpose: Simulation and learning logic require transparent, testable feature derivation rather than opaque or null-filled fields.
Output: Extended scraper, mapping transform, mapping tests, and full documentation.

**Living docs:** Deeper stats pipelines and non-stats sources — [`docs/data/nba-stats-stack-delta-todos.md`](../../../docs/data/nba-stats-stack-delta-todos.md), [`docs/data/external-data-sources.md`](../../../docs/data/external-data-sources.md).
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@server/scripts/scrape_nba_stats.py
@shared/types.ts
@server/services/aggregation.ts
</context>

<tasks>
<task type="auto">
  <name>Task 1: Extend nba_api scraper output to include mapping-ready season fields</name>
  <files>server/scripts/scrape_nba_stats.py</files>
  <action>
1. Add extraction of season-average and context fields required for all PlayerFeatures mappings.
2. Normalize units and null handling (per-game, percentage, rate stats).
3. Emit structured JSON payload consumed by TypeScript mapping service.
  </action>
  <verify>
Run scraper and inspect sample output to confirm all required fields exist with consistent numeric types.
  </verify>
  <done>Scraper provides complete upstream data for PlayerFeatures transformation.</done>
</task>
<task type="auto">
  <name>Task 2: Implement deterministic 30-field PlayerFeatures mapping and tests</name>
  <files>server/services/playerFeaturesMapping.ts, server/services/playerFeaturesMapping.test.ts, shared/schemas.ts</files>
  <action>
1. Implement mapping function covering every PlayerFeatures field with explicit formulas/default rules.
2. Add tests asserting all 30 target fields are present and numeric within expected ranges.
3. Fail tests when any field is unmapped or silently null-filled.
  </action>
  <verify>
Run mapping tests and confirm a hard failure occurs if any PlayerFeatures key is removed from transform output.
  </verify>
  <done>Mapping implementation is complete, deterministic, and protected by coverage tests.</done>
</task>
<task type="auto">
  <name>Task 3: Publish field-by-field mapping documentation synced to code</name>
  <files>docs/data/player-feature-mapping.md</files>
  <action>
1. Document each of the 30 features with:
   - source field(s)
   - transform formula
   - fallback/default rule.
2. Include examples for representative player archetypes.
3. Add doc section describing how tests enforce mapping completeness.
  </action>
  <verify>
Cross-check docs against mapping function and tests; verify no undocumented fields remain.
  </verify>
  <done>Mapping documentation is complete, reviewable, and aligned with implementation.</done>
</task>
</tasks>

<verification>
- [ ] Scraper output includes all fields required for feature mapping.
- [ ] Transform emits all 30 PlayerFeatures fields with deterministic values.
- [ ] Tests fail on missing or null-filled mappings.
- [ ] Documentation fully matches implemented transform formulas.
</verification>

<success_criteria>
- DATA-04 and DATA-05 are complete with code + docs + tests.
- Feature derivation pipeline is explicit, maintainable, and auditable.
- Phase 3 is complete and ready for lesson/offseason consumers.
</success_criteria>

<output>
After completion, create `.planning/phases/03-data-layer/03-03-SUMMARY.md`
</output>
