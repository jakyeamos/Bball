---
phase: 02-infrastructure-supabase-and-auth
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01-PLAN
files_modified:
  - server/supabase/migrations/0001_core_tables.sql
  - server/supabase/migrations/0002_rls_policies.sql
  - server/src/routes/internal/rlsProbe.ts
  - server/src/routes/internal/accountUpgrade.ts
  - shared/schemas.ts
autonomous: true
must_haves:
  truths:
    - "All required Phase 2 tables exist with explicit ownership columns and indexes."
    - "RLS policies enforce per-user access for anonymous and authenticated sessions."
    - "Anonymous-to-email upgrade preserves ownership continuity without data loss."
  artifacts:
    - path: "server/supabase/migrations/0001_core_tables.sql"
      provides: "Schema definitions for users, lesson_progress, daily_challenge_attempts, offseason_runs, coach_profiles."
    - path: "server/supabase/migrations/0002_rls_policies.sql"
      provides: "Enable RLS and define read/write policies for user-scoped data."
    - path: "server/src/routes/internal/accountUpgrade.ts"
      provides: "Upgrade endpoint that links anonymous session to account credentials while preserving user ownership."
  key_links:
    - from: "server/src/routes/internal/rlsProbe.ts"
      to: "server/supabase/migrations/0002_rls_policies.sql"
      via: "runtime policy behavior checks"
      pattern: "policy"
    - from: "server/src/routes/internal/accountUpgrade.ts"
      to: "shared/schemas.ts"
      via: "validated upgrade payload"
      pattern: "upgrade"
---

<objective>
Deliver INFRA-03 and INFRA-04 by implementing the Supabase schema, RLS policy layer, and safe anonymous-to-account identity upgrade path.

Purpose: Persistence and auth only become reliable once ownership boundaries are enforced and upgrade continuity is proven.
Output: Migration files, policy probes, and account-upgrade route with verification workflow.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@server/src/lib/supabaseAdmin.ts
@shared/schemas.ts
</context>

<tasks>
<task type="auto">
  <name>Task 1: Create core table migrations with explicit ownership and constraints</name>
  <files>server/supabase/migrations/0001_core_tables.sql</files>
  <action>
1. Create tables: `users`, `lesson_progress`, `daily_challenge_attempts`, `offseason_runs`, `coach_profiles`.
2. Add `user_id` ownership columns where user-scoped data is stored.
3. Add unique/index constraints for high-volume query paths:
   - lesson progress by `(user_id, lesson_id)`
   - daily attempts by `(user_id, challenge_date)`
   - offseason runs by `(user_id, run_id)`.
4. Add created/updated timestamps and default values for migration safety.
  </action>
  <verify>
Apply migration in local Supabase instance and run schema introspection queries to confirm tables, indexes, and constraints.
  </verify>
  <done>Core schema is in place and structurally ready for RLS enforcement.</done>
</task>
<task type="auto">
  <name>Task 2: Implement RLS policies and runtime policy probe route</name>
  <files>server/supabase/migrations/0002_rls_policies.sql, server/src/routes/internal/rlsProbe.ts</files>
  <action>
1. Enable RLS for all user-scoped tables.
2. Add policies for authenticated and anonymous users so each user can read/write only rows where `auth.uid() = user_id`.
3. Create internal probe route that runs policy checks for three-session matrix:
   - anonymous user
   - authenticated user
   - unauthenticated request.
4. Log clear pass/fail markers for each matrix scenario.
  </action>
  <verify>
Use three separate sessions to create/read rows and confirm cross-user reads/writes are blocked while self access succeeds.
  </verify>
  <done>RLS behavior is tested and enforceable across all required data paths.</done>
</task>
<task type="auto">
  <name>Task 3: Implement account-upgrade route that preserves user ownership</name>
  <files>server/src/routes/internal/accountUpgrade.ts, shared/schemas.ts</files>
  <action>
1. Define validated upgrade payload schema in `shared/schemas.ts`.
2. Create backend route that links anonymous identity to email/password credentials using Supabase auth APIs.
3. Ensure existing rows remain associated with the same effective `user_id` after upgrade.
4. Return upgrade verification metadata (pre/post ids and migrated row counts) for audit.
  </action>
  <verify>
Perform before/after queries on progress and daily tables around upgrade flow; confirm zero row loss and stable ownership.
  </verify>
  <done>Anonymous-to-account upgrade path is implemented and data continuity is verified.</done>
</task>
</tasks>

<verification>
- [ ] Migration files apply cleanly with expected tables and indexes.
- [ ] RLS policy matrix passes for anonymous/authenticated/unauthenticated sessions.
- [ ] Cross-user read/write attempts are denied.
- [ ] Upgrade flow keeps existing progress rows attached to same effective user.
</verification>

<success_criteria>
- INFRA-03 schema + RLS is complete and demonstrably enforced.
- INFRA-04 upgrade continuity works with no row loss.
- Phase 2 persistence layer is safe to expose to higher-level product features.
</success_criteria>

<output>
After completion, create `.planning/phases/02-infrastructure-supabase-and-auth/02-02-SUMMARY.md`
</output>
