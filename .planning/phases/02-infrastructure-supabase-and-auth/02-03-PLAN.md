---
phase: 02-infrastructure-supabase-and-auth
plan: 03
type: execute
wave: 3
depends_on:
  - 02-02-PLAN
files_modified:
  - shared/schemas.ts
  - shared/index.ts
  - client/src/lib/queryClient.ts
  - client/src/services/api.ts
  - client/src/features/learning/queries.ts
  - server/src/routes/lessons.ts
  - server/src/routes/progress.ts
  - server/src/middleware/validateBody.ts
autonomous: true
must_haves:
  truths:
    - "Learning API payloads are validated with shared Zod contracts on request and response boundaries."
    - "Client learning calls use TanStack Query hooks instead of ad-hoc fetch state management."
    - "Invalid payloads return deterministic 400 responses with structured validation errors."
  artifacts:
    - path: "shared/schemas.ts"
      provides: "Single source of truth schemas for lesson, progress, and offseason payload shapes."
    - path: "client/src/features/learning/queries.ts"
      provides: "TanStack Query hooks for list, detail, and mutation workflows."
    - path: "server/src/middleware/validateBody.ts"
      provides: "Reusable request validation wrapper returning standardized 400 errors."
  key_links:
    - from: "server/src/routes/lessons.ts"
      to: "shared/schemas.ts"
      via: "body and response parsing"
      pattern: "safeParse"
    - from: "client/src/features/learning/queries.ts"
      to: "client/src/services/api.ts"
      via: "query/mutation fetch adapters"
      pattern: "useQuery"
---

<objective>
Deliver INFRA-05 and INFRA-06 by implementing shared Zod contracts and migrating learning-layer HTTP flows to TanStack Query.

Purpose: Ensure data-shape correctness and predictable client async behavior before content and progression phases build on top.
Output: Shared schemas, request validation middleware, query client setup, and migrated learning hooks.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@client/src/services/api.ts
@shared/types.ts
@server/src/routes/lobbies.ts
</context>

<tasks>
<task type="auto">
  <name>Task 1: Define shared schema contracts and reusable server validation middleware</name>
  <files>shared/schemas.ts, shared/index.ts, server/src/middleware/validateBody.ts, server/src/routes/lessons.ts, server/src/routes/progress.ts</files>
  <action>
1. Add Zod schemas for lesson records, progress writes, and offseason payload primitives.
2. Export schemas from `shared/index.ts` for client/server imports.
3. Create middleware/helper that validates request body/query params and sends `400` with standardized `issues` list on failure.
4. Apply middleware to lesson and progress routes.
  </action>
  <verify>
Send malformed requests to validated endpoints and confirm 400 responses include Zod issue paths and messages.
  </verify>
  <done>Server routes enforce shared contracts and reject malformed payloads deterministically.</done>
</task>
<task type="auto">
  <name>Task 2: Install and configure TanStack Query client at app root</name>
  <files>client/src/lib/queryClient.ts, client/src/index.tsx, client/package.json</files>
  <action>
1. Install `@tanstack/react-query` (v5) in client workspace.
2. Create singleton `QueryClient` with retry/backoff defaults appropriate for API usage.
3. Wrap app root with `QueryClientProvider`.
4. Add devtools integration only in development mode.
  </action>
  <verify>
Launch client and confirm query cache initializes without runtime warnings; inspect query behavior in devtools.
  </verify>
  <done>TanStack Query is configured and available for learning-layer API hooks.</done>
</task>
<task type="auto">
  <name>Task 3: Migrate learning-layer REST calls to query/mutation hooks</name>
  <files>client/src/services/api.ts, client/src/features/learning/queries.ts, client/src/pages/HomePage.tsx, client/src/pages/LessonPage.tsx</files>
  <action>
1. Replace ad-hoc fetch handling for lesson list/detail/progress mutation paths with `useQuery`/`useMutation` hooks.
2. Parse request/response data with shared schemas.
3. Invalidate relevant query keys after mutation success.
4. Normalize API errors into UI-safe message format.
  </action>
  <verify>
Run manual flow: fetch lessons, open lesson detail, submit progress; confirm cache updates and no stale UI states.
  </verify>
  <done>Learning-layer network calls are query-driven, schema-validated, and cache-consistent.</done>
</task>
</tasks>

<verification>
- [ ] Shared Zod schemas compile and export cleanly.
- [ ] Invalid requests return structured 400 validation responses.
- [ ] Query client is active in root app provider stack.
- [ ] Learning read/write flows run through TanStack Query hooks.
</verification>

<success_criteria>
- INFRA-05 and INFRA-06 are complete with practical route/hook adoption.
- API boundary validation and client async state are standardized.
- Phase 2 is fully complete and ready for dependent Phases 4 and 5.
</success_criteria>

<output>
After completion, create `.planning/phases/02-infrastructure-supabase-and-auth/02-03-SUMMARY.md`
</output>
