---
phase: 02-infrastructure-supabase-and-auth
plan: 01
type: execute
wave: 1
depends_on:
  - 01-05-PLAN
files_modified:
  - client/package.json
  - client/src/index.tsx
  - client/src/lib/supabaseClient.ts
  - client/src/context/AuthContext.tsx
  - server/package.json
  - server/index.ts
  - server/src/lib/supabaseAdmin.ts
  - .env.example
autonomous: true
must_haves:
  truths:
    - "Client and server use explicit Supabase env variables and fail fast when missing."
    - "Anonymous sign-in is executed once on app boot and yields a stable Supabase user id."
    - "Auth bootstrap is observable with deterministic logs and non-blocking UI fallback."
  artifacts:
    - path: "client/src/lib/supabaseClient.ts"
      provides: "Typed Supabase browser client with env validation and singleton export."
    - path: "client/src/context/AuthContext.tsx"
      provides: "Anonymous session bootstrap, auth state exposure, and loading/error boundaries."
    - path: "server/src/lib/supabaseAdmin.ts"
      provides: "Service-role Supabase client for backend-only operations."
  key_links:
    - from: "client/src/index.tsx"
      to: "client/src/context/AuthContext.tsx"
      via: "top-level provider composition"
      pattern: "AuthProvider"
    - from: "server/index.ts"
      to: "server/src/lib/supabaseAdmin.ts"
      via: "startup env validation and client import"
      pattern: "SUPABASE"
---

<objective>
Implement INFRA-01 and INFRA-02 foundations by wiring Supabase clients in both workspaces and bootstrapping anonymous auth for every first-load visitor.

Purpose: Phase 2 must start with a reliable identity layer before schema, RLS, and migration work can be validated.
Output: Shared auth bootstrap flow, environment contract, and deterministic anonymous session behavior.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@client/src/index.tsx
@server/index.ts
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add Supabase dependencies and explicit env contract</name>
  <files>client/package.json, server/package.json, .env.example</files>
  <action>
1. Add `@supabase/supabase-js` to both client and server workspaces.
2. Define required env keys:
   - Client: `VITE_SUPABASE_URL`, `VITE_SUPABASE_ANON_KEY`
   - Server: `SUPABASE_URL`, `SUPABASE_SERVICE_ROLE_KEY`
3. Update `.env.example` with all keys and short comments describing scope.
4. Add startup guards that throw clear errors when required keys are missing.
  </action>
  <verify>
Run `npm install` then run `npm run dev`; verify startup fails with explicit missing-env messages when keys are absent and boots when keys are provided.
  </verify>
  <done>Dependency and env contracts are present and validated at runtime in both workspaces.</done>
</task>
<task type="auto">
  <name>Task 2: Create client auth bootstrap with anonymous sign-in on first load</name>
  <files>client/src/lib/supabaseClient.ts, client/src/context/AuthContext.tsx, client/src/index.tsx</files>
  <action>
1. Create singleton browser client in `supabaseClient.ts`.
2. Implement `AuthContext` with:
   - `session`, `user`, `isLoading`, `error`
   - one-time boot method calling `supabase.auth.getSession()` then `signInAnonymously()` when no session exists.
3. Attach `onAuthStateChange` listener and cleanup on unmount.
4. Wrap root app with `AuthProvider`.
5. Ensure bootstrap does not block full app rendering forever on failure; provide recoverable error state.
  </action>
  <verify>
Open app in a clean browser profile, inspect Supabase auth state, and confirm a durable anonymous user id appears and persists after reload.
  </verify>
  <done>Anonymous user identity is auto-created and persisted without manual sign-in UX.</done>
</task>
<task type="auto">
  <name>Task 3: Add server-side Supabase admin client and startup validation hooks</name>
  <files>server/src/lib/supabaseAdmin.ts, server/index.ts</files>
  <action>
1. Create service-role Supabase client using server env values only.
2. Add startup validation helper ensuring service-role key is never leaked to client logs.
3. Export admin client for upcoming Phase 2 schema and migration routes.
4. Add concise startup log indicating Supabase admin client readiness.
  </action>
  <verify>
Run `npm run dev --workspace=server` and confirm startup log includes Supabase admin readiness without exposing secrets.
  </verify>
  <done>Backend can securely perform privileged Supabase operations required by later plans.</done>
</task>
</tasks>

<verification>
- [ ] Client and server install and import `@supabase/supabase-js` successfully.
- [ ] Required env keys are documented and runtime-validated.
- [ ] First-load anonymous sign-in runs once and produces persistent user id.
- [ ] Server service-role client initializes without secret leakage.
</verification>

<success_criteria>
- INFRA-01 baseline env and client setup is complete.
- INFRA-02 anonymous auth bootstrap works across reloads.
- Phase 2 can proceed to schema/RLS work without auth boot ambiguity.
</success_criteria>

<output>
After completion, create `.planning/phases/02-infrastructure-supabase-and-auth/02-01-SUMMARY.md`
</output>
