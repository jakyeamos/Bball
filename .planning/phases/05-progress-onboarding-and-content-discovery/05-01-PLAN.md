---
phase: 05-progress-onboarding-and-content-discovery
plan: 01
type: execute
wave: 1
depends_on:
  - 04-05-PLAN
files_modified:
  - client/src/features/progress/useLessonProgress.ts
  - client/src/features/progress/progressStorage.ts
  - server/src/routes/progress.ts
  - shared/schemas.ts
  - client/src/pages/LessonPage.tsx
  - shared/src/config/featureFlags.ts
autonomous: true
must_haves:
  truths:
    - "Lesson completion persists across sessions for anonymous and authenticated users."
    - "Anonymous path uses localStorage and account path uses Supabase without API contract drift."
    - "Progress feature rollout is controlled via explicit feature flags."
  artifacts:
    - path: "client/src/features/progress/useLessonProgress.ts"
      provides: "Unified client hook handling local and remote progress persistence."
    - path: "client/src/features/progress/progressStorage.ts"
      provides: "Storage abstraction for anonymous and authenticated user modes."
    - path: "server/src/routes/progress.ts"
      provides: "Validated progress read/write API for account-backed persistence."
  key_links:
    - from: "client/src/pages/LessonPage.tsx"
      to: "client/src/features/progress/useLessonProgress.ts"
      via: "lesson completion updates"
      pattern: "complete"
    - from: "client/src/features/progress/progressStorage.ts"
      to: "server/src/routes/progress.ts"
      via: "authenticated persistence adapter"
      pattern: "progress"
---

<objective>
Deliver LEARN-08 by implementing dual-path progress persistence with a single client contract and controlled rollout.

Purpose: Reliable progress memory is required before personalization and content discovery features can feel coherent.
Output: Unified progress hook, storage adapters, server route validation, and rollout gating.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@client/src/pages/LessonPage.tsx
@server/src/routes/lessons.ts
</context>

<tasks>
<task type="auto">
  <name>Task 1: Build unified progress storage adapter for anonymous and account users</name>
  <files>client/src/features/progress/progressStorage.ts, client/src/features/progress/useLessonProgress.ts</files>
  <action>
1. Implement localStorage adapter for anonymous users.
2. Implement API-backed adapter for authenticated users.
3. Expose unified interface: read progress, upsert completion, list completions.
4. Add mode-switch handling when a user upgrades from anonymous to account.
  </action>
  <verify>
Complete lessons in both user modes and confirm reload persistence behavior remains correct in each mode.
  </verify>
  <done>Progress adapter works consistently for both persistence strategies.</done>
</task>
<task type="auto">
  <name>Task 2: Implement validated progress API and lesson integration</name>
  <files>server/src/routes/progress.ts, shared/schemas.ts, client/src/pages/LessonPage.tsx</files>
  <action>
1. Add progress CRUD endpoints with shared Zod validation.
2. Integrate completion writes from lesson interactions.
3. Add defensive deduplication for repeated completion submissions.
4. Return normalized progress state to client after writes.
  </action>
  <verify>
Submit repeated completion events for same lesson and verify idempotent persisted state.
  </verify>
  <done>Lesson completion persistence is API-safe and idempotent.</done>
</task>
<task type="auto">
  <name>Task 3: Add feature flag controls and regression tests for progress flows</name>
  <files>shared/src/config/featureFlags.ts, client/src/features/progress/useLessonProgress.ts, server/src/routes/progress.ts</files>
  <action>
1. Add `progressPersistenceEnabled` flag.
2. Gate write operations and UI indicators when disabled.
3. Add regression tests for anonymous, authenticated, and mode-switch cases.
  </action>
  <verify>
Toggle flag and verify progress writes/reads behave as expected for enabled and disabled modes.
  </verify>
  <done>User-facing progress behavior is safely rollout-controlled and regression-tested.</done>
</task>
</tasks>

<verification>
- [ ] Completion data persists across browser restarts for anonymous and account users.
- [ ] Progress writes are idempotent for repeated submissions.
- [ ] Shared schema validation protects progress API boundary.
- [ ] Feature flags correctly control persistence behavior exposure.
</verification>

<success_criteria>
- LEARN-08 is complete and stable.
- Phase 5 personalization work can depend on trustworthy progress state.
</success_criteria>

<output>
After completion, create `.planning/phases/05-progress-onboarding-and-content-discovery/05-01-SUMMARY.md`
</output>
