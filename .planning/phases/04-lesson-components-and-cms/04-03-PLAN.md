---
phase: 04-lesson-components-and-cms
plan: 03
type: execute
wave: 2
depends_on:
  - 02-02-PLAN
  - 04-01-PLAN
files_modified:
  - client/src/pages/admin/AdminLessonsPage.tsx
  - client/src/components/admin/LessonEditorForm.tsx
  - server/src/routes/adminLessons.ts
  - server/src/middleware/requireAdmin.ts
  - shared/schemas.ts
  - shared/src/config/featureFlags.ts
autonomous: true
must_haves:
  truths:
    - "Admin users can create, edit, publish, and unpublish lessons with complete required fields."
    - "Non-admin users are blocked from admin routes and APIs."
    - "Lesson authoring changes are feature-flag gated for rollout safety."
  artifacts:
    - path: "client/src/pages/admin/AdminLessonsPage.tsx"
      provides: "Admin lesson management UI for create/edit/publish workflows."
    - path: "server/src/routes/adminLessons.ts"
      provides: "Validated lesson CRUD endpoints with admin-only access control."
    - path: "server/src/middleware/requireAdmin.ts"
      provides: "Reusable route guard enforcing admin role checks."
  key_links:
    - from: "client/src/components/admin/LessonEditorForm.tsx"
      to: "server/src/routes/adminLessons.ts"
      via: "validated form payload submission"
      pattern: "publish"
    - from: "server/src/routes/adminLessons.ts"
      to: "server/src/middleware/requireAdmin.ts"
      via: "route-level authorization"
      pattern: "admin"
---

<objective>
Deliver CMS-01, CMS-02, and CMS-05 by shipping secure lesson authoring/edit/publish tooling with role-protected API and UI surfaces.

Purpose: The lesson platform cannot scale without reliable admin authoring workflows and strict access control.
Output: Admin lesson editor UI, backend CRUD endpoints, and enforced auth role guards.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@client/src/pages/LobbyPage.tsx
@server/managers/sessionManager.ts
</context>

<tasks>
<task type="auto">
  <name>Task 1: Build admin lesson editor UI with full required field support</name>
  <files>client/src/pages/admin/AdminLessonsPage.tsx, client/src/components/admin/LessonEditorForm.tsx, shared/schemas.ts</files>
  <action>
1. Implement create/edit form covering title, role lens, difficulty, YouTube data, takeaway, interaction type, answer key, and tags.
2. Add validation feedback tied to shared Zod schemas.
3. Add publish/unpublish controls with optimistic UI updates and error rollback.
  </action>
  <verify>
Create and edit lesson drafts in UI, publish/unpublish them, and confirm persisted state changes reload correctly.
  </verify>
  <done>CMS-01 and CMS-02 UI workflows are complete and schema-validated.</done>
</task>
<task type="auto">
  <name>Task 2: Implement admin-only lesson CRUD APIs with strict route guards</name>
  <files>server/src/routes/adminLessons.ts, server/src/middleware/requireAdmin.ts, shared/schemas.ts</files>
  <action>
1. Create backend routes for lesson create/update/publish/unpublish.
2. Apply admin guard middleware to all admin lesson endpoints.
3. Return structured validation/auth errors for frontend handling.
4. Add audit logging for publish/unpublish actions.
  </action>
  <verify>
Call endpoints as admin and non-admin users; verify admin success and non-admin denial for all operations.
  </verify>
  <done>CMS-05 is complete with enforceable admin-only access to authoring operations.</done>
</task>
<task type="auto">
  <name>Task 3: Add rollout gating and regression checks for CMS lesson workflows</name>
  <files>shared/src/config/featureFlags.ts, client/src/pages/admin/AdminLessonsPage.tsx, server/src/routes/adminLessons.ts</files>
  <action>
1. Add `cmsLessonAuthoringEnabled` feature flag.
2. Hide admin authoring UI/routes when flag is disabled.
3. Add smoke tests for create/edit/publish under flag-on and flag-off scenarios.
  </action>
  <verify>
Toggle feature flag and confirm CMS lesson authoring routes and UI correctly enable/disable.
  </verify>
  <done>Feature-flag policy hook is complete for user-facing/admin-facing CMS release control.</done>
</task>
</tasks>

<verification>
- [ ] Admin can create/edit/publish/unpublish lessons from CMS.
- [ ] Non-admin access to `/admin` and admin APIs is blocked.
- [ ] Shared validation schema errors are surfaced clearly in UI/API responses.
- [ ] Authoring workflow release is gated by feature flag.
</verification>

<success_criteria>
- CMS-01, CMS-02, and CMS-05 are complete.
- Lesson authoring is secure, reliable, and release-controlled.
</success_criteria>

<output>
After completion, create `.planning/phases/04-lesson-components-and-cms/04-03-SUMMARY.md`
</output>
