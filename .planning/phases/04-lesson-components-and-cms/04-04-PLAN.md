---
phase: 04-lesson-components-and-cms
plan: 04
type: execute
wave: 3
depends_on:
  - 04-03-PLAN
files_modified:
  - client/src/pages/admin/AdminDailyChallengePage.tsx
  - client/src/pages/admin/AdminTagsPage.tsx
  - client/src/components/admin/TagManager.tsx
  - server/src/routes/adminDailyChallenge.ts
  - server/src/routes/adminTags.ts
  - shared/schemas.ts
  - shared/src/config/featureFlags.ts
autonomous: true
must_haves:
  truths:
    - "Admin can schedule daily challenges by date with lesson and answer-key association."
    - "Admin can create, rename, and delete content tags used by library filters."
    - "Scheduling and tag management are protected by admin access and release flags."
  artifacts:
    - path: "client/src/pages/admin/AdminDailyChallengePage.tsx"
      provides: "Daily challenge calendar scheduling UI."
    - path: "client/src/pages/admin/AdminTagsPage.tsx"
      provides: "Tag administration interface for creation and lifecycle changes."
    - path: "server/src/routes/adminTags.ts"
      provides: "Tag CRUD API for CMS with validation and auth checks."
  key_links:
    - from: "client/src/pages/admin/AdminDailyChallengePage.tsx"
      to: "server/src/routes/adminDailyChallenge.ts"
      via: "schedule payload submission"
      pattern: "date"
    - from: "client/src/components/admin/TagManager.tsx"
      to: "server/src/routes/adminTags.ts"
      via: "tag create/rename/delete"
      pattern: "tag"
---

<objective>
Deliver CMS-03 and CMS-04 by implementing admin scheduling for daily challenges and full tag lifecycle management.

Purpose: Editorial operations require stable scheduling and taxonomy management to keep discovery and engagement layers coherent.
Output: Admin calendar scheduling UI/API, tag management UI/API, and rollout controls.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@client/src/pages/admin/AdminLessonsPage.tsx
@server/src/routes/adminLessons.ts
</context>

<tasks>
<task type="auto">
  <name>Task 1: Implement daily challenge scheduling interface and API</name>
  <files>client/src/pages/admin/AdminDailyChallengePage.tsx, server/src/routes/adminDailyChallenge.ts, shared/schemas.ts</files>
  <action>
1. Build date-based scheduling UI selecting lesson and challenge prompt/answer key.
2. Prevent duplicate schedules for same date.
3. Validate schedule payloads server-side.
4. Return preview endpoint for future scheduled challenges.
  </action>
  <verify>
Schedule challenge entries across multiple dates and verify retrieval by date with no duplicates.
  </verify>
  <done>CMS-03 is complete with stable scheduling workflows and validation guards.</done>
</task>
<task type="auto">
  <name>Task 2: Implement tag lifecycle management workflows</name>
  <files>client/src/pages/admin/AdminTagsPage.tsx, client/src/components/admin/TagManager.tsx, server/src/routes/adminTags.ts</files>
  <action>
1. Add UI and API support for create, rename, and delete tag actions.
2. Prevent destructive delete when tag is still attached to active lessons unless reassignment path is provided.
3. Expose tag usage counts for safe cleanup decisions.
  </action>
  <verify>
Create/rename/delete tags and verify affected lesson tag relationships and usage counters remain consistent.
  </verify>
  <done>CMS-04 is complete with safe tag lifecycle operations.</done>
</task>
<task type="auto">
  <name>Task 3: Gate admin scheduling and tag features behind flags</name>
  <files>shared/src/config/featureFlags.ts, client/src/pages/admin/AdminDailyChallengePage.tsx, client/src/pages/admin/AdminTagsPage.tsx</files>
  <action>
1. Add `cmsDailySchedulingEnabled` and `cmsTagManagementEnabled` flags.
2. Hide routes/components when flags disabled.
3. Add smoke tests for flag-off fallback behavior.
  </action>
  <verify>
Toggle flags and confirm admin screens and API entry points follow expected exposure behavior.
  </verify>
  <done>Feature flag policy hook is complete for CMS scheduling and taxonomy features.</done>
</task>
</tasks>

<verification>
- [ ] Daily challenge schedule entries can be created and queried by date.
- [ ] Duplicate date schedules are prevented.
- [ ] Tags can be created, renamed, and deleted safely.
- [ ] Feature flags control scheduling/tag admin surface visibility.
</verification>

<success_criteria>
- CMS-03 and CMS-04 are complete with production-safe admin UX and API behavior.
- Editorial scheduling and taxonomy ops are ready for launch preparation.
</success_criteria>

<output>
After completion, create `.planning/phases/04-lesson-components-and-cms/04-04-SUMMARY.md`
</output>
