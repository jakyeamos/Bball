---
phase: 04-lesson-components-and-cms
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01-PLAN
files_modified:
  - client/src/components/lesson/PausePredict.tsx
  - client/src/components/lesson/PausePredictFallback.tsx
  - client/src/components/lesson/ScenarioSimulation.tsx
  - client/src/components/lesson/LearnMore.tsx
  - client/src/pages/LessonPage.tsx
  - client/src/features/lesson/lessonStateMachine.ts
  - shared/schemas.ts
  - shared/src/config/featureFlags.ts
autonomous: true
must_haves:
  truths:
    - "Pause-and-predict supports play, auto-pause, answer, explanation, and resume flow."
    - "Video unavailable and iframe API failure states are handled without broken UI."
    - "Scenario simulation and Learn More components work with shared lesson schemas."
  artifacts:
    - path: "client/src/components/lesson/PausePredict.tsx"
      provides: "Core pause-and-predict interaction component and answer reveal lifecycle."
    - path: "client/src/features/lesson/lessonStateMachine.ts"
      provides: "Deterministic interaction state transitions for lesson runtime components."
    - path: "client/src/components/lesson/ScenarioSimulation.tsx"
      provides: "Process-based scenario scoring and explanation display."
  key_links:
    - from: "client/src/pages/LessonPage.tsx"
      to: "client/src/features/lesson/lessonStateMachine.ts"
      via: "runtime interaction state orchestration"
      pattern: "state"
    - from: "client/src/components/lesson/PausePredict.tsx"
      to: "client/src/components/lesson/PausePredictFallback.tsx"
      via: "error-state fallback handoff"
      pattern: "fallback"
---

<objective>
Deliver LEARN-03, LEARN-04, LEARN-05, and LEARN-06 by implementing advanced lesson interaction components and robust failure-state behavior.

Purpose: Core teaching value requires interactive exercises that remain reliable under real-world video/API failures.
Output: Pause-predict flow, scenario simulation, Learn More expansion, and runtime state machine.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@client/src/pages/QuarterCoachingPage.tsx
@client/src/services/websocket.ts
</context>

<tasks>
<task type="auto">
  <name>Task 1: Implement pause-and-predict with deterministic interaction state machine</name>
  <files>client/src/components/lesson/PausePredict.tsx, client/src/features/lesson/lessonStateMachine.ts, client/src/pages/LessonPage.tsx</files>
  <action>
1. Define state phases: `playing -> pausedForQuestion -> answering -> reveal -> resumed`.
2. Trigger auto-pause at configured timestamp and lock answer input to single submit path.
3. Reveal explanation after submit and allow resume.
4. Persist interaction completion event for later progress tracking.
  </action>
  <verify>
Run lesson interaction tests and confirm each state transition occurs once per attempt with no dead-end states.
  </verify>
  <done>LEARN-03 is complete with stable pause-and-predict progression.</done>
</task>
<task type="auto">
  <name>Task 2: Add graceful failure states for removed videos and iframe API failures</name>
  <files>client/src/components/lesson/PausePredictFallback.tsx, client/src/components/lesson/PausePredict.tsx</files>
  <action>
1. Detect video unavailable responses and render fallback card with teaching summary.
2. Detect iframe initialization/timeouts and render retry/fallback UI.
3. Keep page layout intact to avoid broken navigation.
4. Add event logging for failure-state occurrence counts.
  </action>
  <verify>
Test with invalid/removed YouTube IDs and forced iframe errors; verify fallback UX renders and app remains usable.
  </verify>
  <done>LEARN-04 is complete with robust unavailable-video and API-failure handling.</done>
</task>
<task type="auto">
  <name>Task 3: Implement scenario simulation and Learn More expandable sections</name>
  <files>client/src/components/lesson/ScenarioSimulation.tsx, client/src/components/lesson/LearnMore.tsx, shared/schemas.ts, shared/src/config/featureFlags.ts</files>
  <action>
1. Build scenario interaction that captures user decision and scores against answer key.
2. Display reasoning-first explanation for correct process.
3. Add optional Learn More section to lesson payload rendering.
4. Gate scenario and Learn More release behind dedicated feature flags.
  </action>
  <verify>
Run scenario fixtures and confirm scoring + explanation output are deterministic; toggle feature flags to validate gated rollout.
  </verify>
  <done>LEARN-05 and LEARN-06 are complete with policy-compliant feature flag gating.</done>
</task>
</tasks>

<verification>
- [ ] Pause-and-predict flow completes end-to-end with controlled state transitions.
- [ ] Video removed and iframe failure states show fallback UI, not blank/broken embeds.
- [ ] Scenario simulation collects response, scores, and explains reasoning.
- [ ] Learn More section expands/collapses and reads from CMS payload.
- [ ] User-facing behavior changes are controlled by explicit flags.
</verification>

<success_criteria>
- LEARN-03 through LEARN-06 are complete and resilient.
- Interaction reliability is sufficient for broad user rollout.
</success_criteria>

<output>
After completion, create `.planning/phases/04-lesson-components-and-cms/04-02-SUMMARY.md`
</output>
