---
phase: 04-lesson-components-and-cms
plan: 01
type: execute
wave: 1
depends_on:
  - 02-03-PLAN
  - 03-03-PLAN
files_modified:
  - client/src/components/lesson/LessonCard.tsx
  - client/src/components/lesson/LessonMeta.tsx
  - client/src/pages/LessonPage.tsx
  - client/src/components/lesson/FilmBreakdown.tsx
  - client/src/components/lesson/AnnotationRail.tsx
  - shared/schemas.ts
  - shared/src/config/featureFlags.ts
autonomous: true
must_haves:
  truths:
    - "Lesson card renders title, role lens badge, difficulty, media, takeaway, and interaction type from CMS data."
    - "Film breakdown page supports timestamped annotations and seek-to behavior."
    - "User-facing lesson surfaces are feature-flag gated before rollout."
  artifacts:
    - path: "client/src/components/lesson/LessonCard.tsx"
      provides: "Primary lesson presentation component for core metadata."
    - path: "client/src/components/lesson/FilmBreakdown.tsx"
      provides: "YouTube embed + timestamp synchronization behavior."
    - path: "shared/src/config/featureFlags.ts"
      provides: "Lesson platform rollout flags for controlled exposure."
  key_links:
    - from: "client/src/pages/LessonPage.tsx"
      to: "client/src/components/lesson/LessonCard.tsx"
      via: "CMS lesson payload render"
      pattern: "lesson"
    - from: "client/src/components/lesson/AnnotationRail.tsx"
      to: "client/src/components/lesson/FilmBreakdown.tsx"
      via: "annotation click seek"
      pattern: "timestamp"
---

<objective>
Deliver LEARN-01 and LEARN-02 by implementing foundational lesson rendering and film-breakdown interaction behavior.

Purpose: Lesson metadata and film annotation UX are the baseline learning surfaces needed before advanced interactions.
Output: Lesson card system, film breakdown page, annotation-seek behavior, and rollout flag controls.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@client/src/pages/HomePage.tsx
@client/src/components/Card.tsx
</context>

<tasks>
<task type="auto">
  <name>Task 1: Build LessonCard and metadata rendering contract</name>
  <files>client/src/components/lesson/LessonCard.tsx, client/src/components/lesson/LessonMeta.tsx, shared/schemas.ts</files>
  <action>
1. Implement card component rendering required fields from CMS payload.
2. Enforce typed props aligned with shared lesson schema.
3. Add visual states for missing optional fields without collapsing layout.
4. Add snapshot/unit tests for all role lens and difficulty combinations.
  </action>
  <verify>
Render card fixtures for Player/Coach/GM lessons and confirm all required labels and metadata are visible.
  </verify>
  <done>LEARN-01 card requirements are fully represented in reusable component form.</done>
</task>
<task type="auto">
  <name>Task 2: Implement film breakdown page with annotation timestamp seeking</name>
  <files>client/src/pages/LessonPage.tsx, client/src/components/lesson/FilmBreakdown.tsx, client/src/components/lesson/AnnotationRail.tsx</files>
  <action>
1. Add YouTube embed wrapper with current-time control hooks.
2. Render annotation list from CMS timestamp data.
3. Implement click-to-seek behavior with active annotation highlighting.
4. Keep playback and annotation state synchronized on manual seek.
  </action>
  <verify>
Load a film lesson and click multiple annotations; verify player jumps to exact timestamps and active state updates.
  </verify>
  <done>LEARN-02 film breakdown workflow operates end-to-end from CMS-configured annotations.</done>
</task>
<task type="auto">
  <name>Task 3: Add rollout feature flags for lesson runtime surfaces</name>
  <files>shared/src/config/featureFlags.ts, client/src/pages/LessonPage.tsx, client/src/pages/HomePage.tsx</files>
  <action>
1. Create `lessonRuntimeEnabled` and `filmBreakdownEnabled` flags.
2. Guard new surfaces behind flags with fallback messaging.
3. Document default flag values for local/staging/production.
  </action>
  <verify>
Toggle flags on/off and confirm route visibility and fallback behavior match expectation.
  </verify>
  <done>Feature flag policy hook is implemented for user-facing lesson runtime changes.</done>
</task>
</tasks>

<verification>
- [ ] LessonCard renders all required metadata fields from schema-validated payloads.
- [ ] Film breakdown annotations seek accurately and update active state.
- [ ] New user-facing lesson features are controllable via flags.
</verification>

<success_criteria>
- LEARN-01 and LEARN-02 are complete.
- Lesson runtime foundation is stable and controllable for rollout.
</success_criteria>

<output>
After completion, create `.planning/phases/04-lesson-components-and-cms/04-01-SUMMARY.md`
</output>
