---
phase: 01-foundation-and-bug-fixes
plan: 04
type: execute
wave: 2
depends_on:
  - 01-03
files_modified:
  - client/src/pages/HomePage.tsx
  - client/src/components/NavBar.tsx
  - client/src/App.tsx
autonomous: true
requirements:
  - FOUND-05
  - FOUND-06
must_haves:
  truths:
    - "Navigating to / renders HomePage with Player IQ, Coach IQ, and GM IQ lanes all visible without any click or scroll required on a 1280px viewport"
    - "Each role lens lane shows a heading, a one-line description, and a call-to-action link"
    - "NavBar is present at the top of every page and contains links to all three role lens sections"
    - "Clicking a NavBar link takes the user to the corresponding role lens section — no dead links"
    - "LobbyPage is reachable at /lobby — the existing draft-sim flow is not broken"
    - "A user on the homepage can navigate to the draft sim (GM IQ lane or NavBar) without being stuck"
  artifacts:
    - path: "client/src/pages/HomePage.tsx"
      provides: "Three-lane homepage with Player IQ, Coach IQ, GM IQ sections"
      exports: ["HomePage"]
    - path: "client/src/components/NavBar.tsx"
      provides: "Top-level navigation component with links to all three role lenses"
      exports: ["NavBar"]
    - path: "client/src/App.tsx"
      provides: "Updated route: / → HomePage, /lobby → LobbyPage (moved from /)"
  key_links:
    - from: "client/src/App.tsx"
      to: "client/src/pages/HomePage.tsx"
      via: "<Route path='/' element={<HomePage />} />"
      pattern: "HomePage"
    - from: "client/src/App.tsx"
      to: "client/src/components/NavBar.tsx"
      via: "NavBar rendered in layout wrapper above <Routes>"
      pattern: "NavBar"
    - from: "client/src/pages/HomePage.tsx"
      to: "client/src/App.tsx routes"
      via: "react-router-dom Link components pointing to /lobby or #gm-iq sections"
      pattern: "Link.*lobby"
---

<objective>
Build the Court Vision homepage (FOUND-05) and top-level NavBar (FOUND-06). The homepage replaces the current LobbyPage at route / and shows all three role lens lanes — Player IQ, Coach IQ, GM IQ — immediately on load with no click or navigation required. The NavBar provides fluid cross-lens navigation from any page. LobbyPage moves to /lobby so the existing draft sim flow remains intact.

Purpose: This is the front door of Court Vision. Every user's first experience is seeing the three learning tracks. The NavBar ensures no lens is a dead end.
Output: HomePage.tsx, NavBar.tsx, and updated routing in App.tsx.
</objective>

<execution_context>
@C:/Users/gspea/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/gspea/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-and-bug-fixes/01-RESEARCH.md
@.planning/phases/01-foundation-and-bug-fixes/01-03-SUMMARY.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md

<interfaces>
<!-- Key patterns and contracts the executor needs. -->

From RESEARCH.md — Route restructure pattern:
```typescript
// client/src/App.tsx Routes — change:
<Route path="/" element={<HomePage />} />          // NEW — was LobbyPage
<Route path="/lobby" element={<LobbyPage />} />    // MOVED — was at "/"
<Route path="/browse" element={<LobbyBrowserPage />} />
// ... rest unchanged
```

From RESEARCH.md — GameRouting guard (no changes needed):
// GameRouting already has `if (!league) return;` — homepage visitors with no active league
// will NOT be redirected. No additional guard needed.

From client/tailwind.config.js (created in Plan 03):
```javascript
// Available cv- tokens to use in components:
// bg-cv-court     (#1a472a — deep court green)
// bg-cv-steel     (#1e293b — dark steel backgrounds)
// bg-cv-navy      (#0f172a — darkest background)
// text-cv-chalk   (#f5f0e8 — light text)
// text-cv-hardwood (#c8a96e — accent text)
// bg-cv-accent    (#f97316 — CTA orange)
// rounded-cv      (0.5rem border radius)
```

From RESEARCH.md — Anti-patterns to avoid:
- Do NOT add Redux or Zustand for homepage state — three role lens lanes are static content
- Do NOT use `useState` for lane visibility — lanes are always visible, no toggle needed
- HomePage and NavBar are static content components

Architecture note from ARCHITECTURE.md:
- Client pages: `PascalCasePage.tsx` in client/src/pages/
- Client components: `PascalCase.tsx` in client/src/components/
- React components use named exports (e.g., `export function HomePage()`)
- Shared package import alias: `@` maps to client/src/
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build HomePage with three role-lens lanes</name>
  <files>client/src/pages/HomePage.tsx</files>
  <action>
    Create client/src/pages/HomePage.tsx as a named export React functional component.

    Structure:
    - Full-width hero section at top: "Court Vision" heading, tagline "Improve your basketball IQ."
    - Three role-lens lanes in a grid (3 columns on lg breakpoint, 1 column on mobile):
      1. Player IQ lane: heading "Player IQ", description "Read the game like a pro. Learn spacing, off-ball movement, and shot selection.", CTA button/link (placeholder for now — link to "#player-iq" anchor or future /lessons/player-iq route)
      2. Coach IQ lane: heading "Coach IQ", description "Understand rotations, clock management, and in-game adjustments.", CTA button/link
      3. GM IQ lane: heading "GM IQ", description "Build rosters, evaluate trades, and think long-term.", CTA button/link pointing to /lobby (entry to draft sim as GM IQ experience)

    Styling guidelines (use cv- tokens from tailwind.config.js):
    - Page background: bg-cv-navy or bg-cv-steel
    - Hero heading: text-cv-chalk, font-display, large (text-4xl lg:text-6xl)
    - Lane cards: bg-cv-steel, border border-cv-court/30, rounded-cv, p-6 lg:p-8, hover:border-cv-court transition
    - Lane headings: text-cv-hardwood or text-cv-chalk, text-xl font-semibold
    - CTA buttons: bg-cv-accent hover:bg-cv-accent-hover text-white, rounded-cv, px-4 py-2
    - Grid: grid grid-cols-1 lg:grid-cols-3 gap-6 max-w-6xl mx-auto px-4

    Use react-router-dom `Link` for the GM IQ CTA that points to /lobby. Use `<a href="#player-iq">` placeholders for Player IQ and Coach IQ CTAs (future routes).

    Add id attributes to each lane div: id="player-iq", id="coach-iq", id="gm-iq" — these are the NavBar anchor targets.

    Named export: `export function HomePage() { ... }`

    NOTE: No useState, no useEffect, no API calls — pure static JSX with Tailwind classes.
  </action>
  <verify>
    <automated>npm run build --workspace=client 2>&1 | grep -E "error|Error" | grep -v "node_modules" | head -10 || echo "BUILD OK"</automated>
  </verify>
  <done>client/src/pages/HomePage.tsx exports HomePage function. npm run build --workspace=client succeeds. Component renders three role-lens lane sections with id attributes player-iq, coach-iq, gm-iq.</done>
</task>

<task type="auto">
  <name>Task 2: Build NavBar and wire App.tsx routing</name>
  <files>client/src/components/NavBar.tsx, client/src/App.tsx</files>
  <action>
    1. Create client/src/components/NavBar.tsx:
       - Named export: `export function NavBar() { ... }`
       - Contains: "Court Vision" brand wordmark (left side), navigation links (right side)
       - Links: "Player IQ" → /#player-iq, "Coach IQ" → /#coach-iq, "GM IQ" → /#gm-iq, "Draft Sim" → /lobby
       - Use react-router-dom `Link` for all links (handles hash anchor routing correctly within SPA)
       - Styling: bg-cv-steel, border-b border-cv-court/30, sticky top-0, z-50
       - Brand text: text-cv-chalk font-display font-bold text-lg
       - Nav links: text-cv-chalk/80 hover:text-cv-chalk text-sm, gap-6 flex
       - Mobile: links collapse to a simple flex row (no hamburger menu needed — the 4 links fit horizontally on most viewports; defer responsive hamburger to a future phase)

    2. Edit client/src/App.tsx:
       a. Add route: `<Route path="/" element={<HomePage />} />` (replaces LobbyPage at /)
       b. Change LobbyPage route: `<Route path="/lobby" element={<LobbyPage />} />` (moved from /)
       c. Import HomePage from '@/pages/HomePage'
       d. Import NavBar from '@/components/NavBar'
       e. Wrap `<Routes>` in a layout div: render `<NavBar />` above `<Routes>` so it appears on all pages
       f. The layout structure in App.tsx should be:
          ```tsx
          return (
            <div className="min-h-screen bg-cv-navy">
              <NavBar />
              <Routes>
                <Route path="/" element={<HomePage />} />
                <Route path="/lobby" element={<LobbyPage />} />
                {/* ... rest of existing routes unchanged ... */}
              </Routes>
              <GameRouting />
              {/* existing DebugOverlay etc. */}
            </div>
          );
          ```

    NOTE: Do NOT modify GameRouting logic. It already guards with `if (!league) return;` so homepage visitors are unaffected. Do NOT remove any existing routes — only add the new / route and move LobbyPage to /lobby.

    After changes: npm run build --workspace=client to confirm no TypeScript errors.
  </action>
  <verify>
    <automated>npm run build --workspace=client 2>&1 | grep -E "error|Error" | grep -v "node_modules" | head -10 || echo "BUILD OK"</automated>
  </verify>
  <done>client/src/components/NavBar.tsx exports NavBar. client/src/App.tsx route / renders HomePage, route /lobby renders LobbyPage. NavBar is rendered on all pages. npm run build --workspace=client succeeds with no TypeScript errors.</done>
</task>

</tasks>

<verification>
npm run build --workspace=client succeeds with no errors.
grep "HomePage" client/src/App.tsx returns the route and import.
grep "path=\"/lobby\"" client/src/App.tsx returns the LobbyPage route.
grep "NavBar" client/src/App.tsx returns the import and JSX usage.
cat client/src/pages/HomePage.tsx | grep "id=\"player-iq\"\|id=\"coach-iq\"\|id=\"gm-iq\"" returns all three lane IDs.
</verification>

<success_criteria>
1. client/src/pages/HomePage.tsx exists with three role-lens lanes (player-iq, coach-iq, gm-iq sections)
2. client/src/components/NavBar.tsx exists with links to all three lenses and /lobby
3. client/src/App.tsx routes / to HomePage and /lobby to LobbyPage
4. NavBar is rendered on all pages (above Routes in layout)
5. npm run build --workspace=client succeeds with no TypeScript errors
</success_criteria>

<output>
After completion, create .planning/phases/01-foundation-and-bug-fixes/01-04-SUMMARY.md
</output>
