---
phase: 01-foundation-and-bug-fixes
plan: 05
type: execute
wave: 3
depends_on:
  - 01-01
  - 01-02
  - 01-03
  - 01-04
files_modified: []
autonomous: false
requirements:
  - FOUND-01
  - FOUND-02
  - FOUND-03
  - FOUND-04
  - FOUND-05
  - FOUND-06
must_haves:
  truths:
    - "Browser tab title reads 'Court Vision' — not 'NBA Draft Sim'"
    - "Homepage shows Player IQ, Coach IQ, GM IQ lanes without any click"
    - "NavBar links navigate to each role lens section"
    - "Quarter coaching game advances through quarter transitions without hanging"
    - "npm test --workspace=server passes — simulation correctness regression test green"
  artifacts:
    - path: "client/src/pages/HomePage.tsx"
      provides: "Three-lane homepage"
    - path: "client/src/components/NavBar.tsx"
      provides: "Top-level navigation"
    - path: "client/tailwind.config.js"
      provides: "Court Vision design tokens"
    - path: "server/services/handlers-v2.ts"
      provides: "initHandlersV2 + real aggregations + quarter handlers"
    - path: "server/src/__tests__/simulation-correctness.test.ts"
      provides: "Monte Carlo regression test"
  key_links:
    - from: "client browser"
      to: "Court Vision homepage"
      via: "navigate to /"
      pattern: "Player IQ.*Coach IQ.*GM IQ"
---

<objective>
Human verification of all Phase 1 success criteria. Claude has completed all four automated plans. This checkpoint confirms the simulation bug is fixed, the quarter WebSocket events work end-to-end in a live browser session, the rebrand is complete, and the homepage + navigation behave correctly.

Purpose: Phase 1 gates Phase 2 and Phase 3. Confirming correctness here prevents propagating any silently broken behavior into the Supabase and Data Layer phases.
Output: Verified phase 1 — all six requirements confirmed.
</objective>

<execution_context>
@C:/Users/gspea/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/gspea/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full test suite and build verification</name>
  <files></files>
  <action>
    Run all automated checks before presenting to user:

    1. npm test --workspace=server
       Expected: simulation-correctness test passes, win rate > 0.60 asserted

    2. npm run build --workspace=shared
       Expected: no TypeScript errors

    3. npm run build --workspace=server
       Expected: no TypeScript errors

    4. npm run build --workspace=client
       Expected: no TypeScript errors

    5. grep -r "NBA Draft Sim\|NBA Draft Simulator" client/src/ client/index.html
       Expected: 0 results (legacy brand strings removed)

    6. grep "READY_FOR_QUARTER" shared/types.ts
       Expected: returns the WS_EVENTS entry

    7. grep "initHandlersV2" server/services/handlers-v2.ts server/managers/socketManager.ts
       Expected: returns the export and call site in both files

    Report all results before proceeding to checkpoint.
  </action>
  <verify>
    <automated>npm test --workspace=server && npm run build --workspace=shared && npm run build --workspace=server && npm run build --workspace=client && echo "ALL CHECKS PASSED"</automated>
  </verify>
  <done>All five automated checks pass with no errors. Results reported to user before checkpoint.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Plans 01–04 complete:
    - Plan 01: Fixed handleSimulateRoundInternal (FOUND-03) — real TeamAggregation objects, Monte Carlo regression test green
    - Plan 02: Wired SUBMIT_QUARTER_COACHING and READY_FOR_QUARTER socket events (FOUND-04)
    - Plan 03: Court Vision rebrand — updated title/meta tags, tailwind.config.js with cv- tokens (FOUND-01, FOUND-02)
    - Plan 04: HomePage with three role-lens lanes, NavBar with cross-lens navigation, route / → HomePage, /lobby → LobbyPage (FOUND-05, FOUND-06)
  </what-built>
  <how-to-verify>
    Run the dev server first: npm run dev (or equivalent start command from project root)

    Check 1 — FOUND-01 (Rebrand):
    - Open http://localhost:5173 (or configured port)
    - Browser tab should read "Court Vision"
    - Page heading should say "Court Vision" — not "NBA Draft Sim"

    Check 2 — FOUND-02 (Design Tokens):
    - On the homepage, right-click a lane card → Inspect
    - In computed styles, verify background-color matches #1e293b (cv-steel) or #0f172a (cv-navy)
    - Verify the orange CTA buttons show #f97316 (cv-accent)

    Check 3 — FOUND-05 (Homepage):
    - Navigate to http://localhost:5173/
    - Without scrolling or clicking, all three lanes should be visible: "Player IQ", "Coach IQ", "GM IQ"

    Check 4 — FOUND-06 (Navigation):
    - Click each NavBar link: Player IQ, Coach IQ, GM IQ — each should scroll to or highlight the correct section
    - Click "Draft Sim" in NavBar — should navigate to /lobby (the draft sim lobby creation page)
    - On /lobby, navigate back to / using browser back button or NavBar — should return to homepage

    Check 5 — FOUND-04 (Quarter WebSocket):
    - Start a game that enters the quarter-by-quarter coaching flow
    - Open browser DevTools → Network → WS tab
    - Submit coaching decisions for a quarter
    - Confirm: game:submit_quarter_coaching event is sent AND game:quarter_result event is received back
    - Confirm: game advances to the next quarter (does not hang indefinitely)

    Check 6 — FOUND-03 (Simulation Correctness — automated, but confirm):
    - The automated check in Task 1 already ran npm test --workspace=server
    - Confirm the test output showed win rate assertion passing (no need to re-run manually)
  </how-to-verify>
  <resume-signal>
    Type "approved" if all 6 checks pass.
    Or describe which checks failed and what you observed — Claude will fix and return to this checkpoint.
  </resume-signal>
</task>

</tasks>

<verification>
All automated build checks pass.
Human confirms all 6 Phase 1 success criteria via live browser session.
</verification>

<success_criteria>
All Phase 1 success criteria from ROADMAP.md are confirmed:
1. Auto-sim with high-rated coaching produces different outcomes from blank stubs (FOUND-03 — automated test green)
2. Quarter coaching flow advances without hanging — socket traffic confirmed (FOUND-04)
3. "Court Vision" in browser tab title, headings, and meta tags — no legacy branding (FOUND-01)
4. Homepage shows Player IQ, Coach IQ, GM IQ lanes on load without navigation (FOUND-05)
5. NavBar enables fluid cross-lens navigation (FOUND-06)
6. cv- design tokens render in browser (FOUND-02)
</success_criteria>

<output>
After completion, create .planning/phases/01-foundation-and-bug-fixes/01-05-SUMMARY.md

Update STATE.md:
- Current position: Phase 1 complete, Phase 2 and Phase 3 ready to start (can be parallelized)
- Update progress percentage

Update ROADMAP.md:
- Mark Phase 1 checkbox complete: [x] Phase 1: Foundation and Bug Fixes
</output>
