---
phase: 09-offseason-simulator-foundation
plan: 01
type: execute
wave: 1
depends_on:
  - 08-02-PLAN
files_modified:
  - shared/src/offseason/schema.ts
  - server/src/offseason/migrations.ts
  - server/src/offseason/stateMachine.ts
  - server/src/routes/offseasonRuns.ts
  - shared/schemas.ts
  - shared/src/config/featureFlags.ts
autonomous: true
must_haves:
  truths:
    - "Offseason run state includes schemaVersion from first persisted record."
    - "Migration helpers upgrade older run payloads to current schema without data loss."
    - "Phase transitions persist state atomically and resume reliably."
  artifacts:
    - path: "shared/src/offseason/schema.ts"
      provides: "Versioned offseason run contract with validators."
    - path: "server/src/offseason/migrations.ts"
      provides: "Schema migration pipeline for saved run upgrades."
    - path: "server/src/routes/offseasonRuns.ts"
      provides: "Persist/resume API for offseason phase transitions."
  key_links:
    - from: "server/src/routes/offseasonRuns.ts"
      to: "shared/src/offseason/schema.ts"
      via: "run payload validation"
      pattern: "schemaVersion"
    - from: "server/src/offseason/stateMachine.ts"
      to: "server/src/offseason/migrations.ts"
      via: "load-and-upgrade before transition"
      pattern: "migrate"
---

<objective>
Deliver OSIM-08 and OSIM-09 by implementing versioned offseason state persistence and migration-safe resume behavior.

Purpose: The full offseason simulator depends on robust save/resume and schema evolution from day one.
Output: Versioned schema, migration engine, phase transition persistence API, and rollout controls.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@server/src/routes/progress.ts
@shared/types.ts
</context>

<tasks>
<task type="auto">
  <name>Task 1: Define offseason run schema with schemaVersion and validation</name>
  <files>shared/src/offseason/schema.ts, shared/schemas.ts</files>
  <action>
1. Define core offseason run structure with explicit phase state and `schemaVersion`.
2. Add validation for all phase payload slices.
3. Export schema helpers for server/client use.
4. Document versioning policy for future field additions.
  </action>
  <verify>
Validate sample run payloads and ensure invalid/missing schemaVersion fails parsing.
  </verify>
  <done>Versioned state contract is established and enforceable.</done>
</task>
<task type="auto">
  <name>Task 2: Implement migration helpers and transition-safe persistence</name>
  <files>server/src/offseason/migrations.ts, server/src/offseason/stateMachine.ts, server/src/routes/offseasonRuns.ts</files>
  <action>
1. Implement migration registry from prior versions to latest.
2. Upgrade run state on load before transition handling.
3. Persist on each phase transition with atomic write semantics.
4. Add transition validation to block invalid phase jumps.
  </action>
  <verify>
Load synthetic v1 payload into v2 runtime and confirm migration output validates and transitions continue.
  </verify>
  <done>Save/resume and migration behavior is reliable across schema versions.</done>
</task>
<task type="auto">
  <name>Task 3: Add feature-flag controls and migration regression tests</name>
  <files>shared/src/config/featureFlags.ts, server/src/offseason/migrations.test.ts, server/src/routes/offseasonRuns.ts</files>
  <action>
1. Add `offseasonFoundationEnabled` flag.
2. Add regression tests for migration and resume-at-phase behavior.
3. Ensure fallback behavior when feature flag is disabled.
  </action>
  <verify>
Run migration test suite and confirm flagged-off state blocks offseason run entry points cleanly.
  </verify>
  <done>Foundation rollout is controlled and migration behavior is regression-protected.</done>
</task>
</tasks>

<verification>
- [ ] Offseason schema includes and enforces schemaVersion.
- [ ] Older run payloads migrate to latest schema.
- [ ] State persists at each phase transition and resumes correctly.
- [ ] Foundation feature flag gates offseason entry.
</verification>

<success_criteria>
- OSIM-08 and OSIM-09 are complete.
- Offseason simulator can safely evolve schema without breaking saved runs.
</success_criteria>

<output>
After completion, create `.planning/phases/09-offseason-simulator-foundation/09-01-SUMMARY.md`
</output>
