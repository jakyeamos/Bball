{
  "name": "nba-draft-sim",
  "private": true,
  "workspaces": [
    "client",
    "server",
    "shared"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=client",
    "build": "npm run build --workspace=shared && npm run build --workspace=client && npm run build --workspace=server",
    "start": "npm run start --workspace=server",
    "lint": "npm run lint --workspace=client && npm run lint --workspace=server",
    "typecheck": "npm run typecheck --workspace=shared && npm run build --workspace=shared && npm run typecheck --workspace=client && npm run typecheck --workspace=server",
    "test": "npm run test --workspace=shared -- --run && npm run test --workspace=server",
    "smoke": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.59.1",
    "@types/cookie-parser": "^1.4.10",
    "@types/cors": "^2.8.19",
    "@types/express": "^4.17.25",
    "@types/node": "^25.0.3",
    "@types/uuid": "^10.0.0",
    "eslint-plugin-anti-slop": "file:../eslint-plugin-anti-slop",
    "socket.io-client": "^4.8.3"
  },
  "dependencies": {
    "@heroicons/react": "^2.2.0"
  }
}
