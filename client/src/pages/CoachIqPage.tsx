import React from 'react';
import { TrackPageLayout } from './TrackPageLayout';

export function CoachIqPage() {
  return (
    <TrackPageLayout
      roleLens="coach"
      headline="Teach structure that still holds under pressure"
      description="Coach IQ lessons focus on rotation rules, lineup communication, and the tactical choices that stabilize possessions on both ends."
    />
  );
}
